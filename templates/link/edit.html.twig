{% extends 'base.html.twig' %}

{% block title %}Modifier un lien{% endblock %}

{% block body %}
    <h1>Modifier un lien</h1>

    {{ include('link/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="link_actions">
        <a href="{{ path('app_link_index') }}">Retour à la liste des liens</a>
        {{ include('link/_delete_form.html.twig') }}
    </div>

{% endblock %}
