{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.password) }}

    <h3>User Roles</h3>
    {{ form_row(form.roles) }}

    <button class="btn">Save</button>
{{ form_end(form) }}
